<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <link rel="stylesheet" href="/style.css" />

      <title>Login</title>
   </head>
   <body>
      <%- include("partials/header") %>
      <main class="flex justify-center">
         <form
            action="login"
            method="post"
            class="bg-color5 p-4 flex flex-col text-white rounded-sm gap-4 min-w-[300px]"
         >
            <label for="email">Email:</label>
            <input
               type="email"
               name="email"
               id="email"
               required
               value="<%= locals.email && locals.email %>"
               class="text-color2"
            />

            <label for="password">Password:</label>
            <input type="password" name="password" id="password" required class="text-color2" />
            <button type="submit">Login</button>
            <%- include("partials/errors") %>
         </form>
      </main>
   </body>
</html>
